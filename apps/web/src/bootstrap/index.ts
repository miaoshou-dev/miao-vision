/**
 * Lightweight browser bootstrap.
 *
 * The landing page should not initialize the legacy BI runtime.
 */
export function initializeApp(): void {
  // Intentionally empty while the browser app is landing-only.
}
