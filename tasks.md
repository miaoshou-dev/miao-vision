# Miao Vision 增长与首次使用优化任务清单

## 1. 使用说明

本清单将 [requirements.md](/Users/guming/dev/gitcode/miao-vision/requirements.md) 和 [design.md](/Users/guming/dev/gitcode/miao-vision/design.md) 拆解为可独立跟踪的交付任务。

- `[Required]`：达到首个可发布版本必须完成。
- `[Optional]`：不阻断首个版本，可在验证核心漏斗后完成。
- `Depends on`：任务开始前必须完成的任务。
- 每个任务完成后应能通过描述中的产出物或验收条件核验。
- 所有任务默认遵守仓库 `AGENTS.md`：不编辑生成目录，不引入必需后端，不把数据分析逻辑放入 Web。

## 2. 交付路线图

```text
T01 需求与现状基线
 ├── T02 内容/版本单一来源
 │    └── T03 示例体验包
 ├── T04 首屏 Report 定位
 ├── T05 示例卡片动作
 └── T06 安装分流设计
       └── T07 安装页面实现
             └── T08 安装验证/诊断

T03 + T07 + 现有 CLI
 └── T09 首次 Report 冒烟流程
       ├── T10 错误修复体验
       └── T11 完成页后续动作
             └── T12 周期性更新验证

T04–T12
 └── T13 Web 可访问性与响应式测试
       └── T14 发布回归与上线验收

T14
 └── T15 匿名漏斗观测（Optional）
```

## 3. 任务总览

| ID | 任务 | 优先级 | 类型 | 依赖 | 状态 |
|---|---|---:|---|---|---|
| T01 | 建立增长基线与验证口径 | P0 | Required | 无 | ☐ |
| T02 | 建立版本与安装内容单一来源 | P0 | Required | T01 | ☐ |
| T03 | 创建 Report 首次体验包 | P0 | Required | T02 | ☐ |
| T04 | 重构首屏为 Report 主入口 | P0 | Required | T01 | ☐ |
| T05 | 改造示例卡片与示例动作 | P0 | Required | T03、T04 | ☐ |
| T06 | 设计宿主安装分流模型 | P0 | Required | T02 | ☐ |
| T07 | 实现安装分流与验证文案 | P0 | Required | T06 | ☐ |
| T08 | 实现统一环境诊断 | P1 | Required | T02、T07 | ☐ |
| T09 | 打通五分钟首次 Report 冒烟路径 | P0 | Required | T03、T07 | ☐ |
| T10 | 完善结构化失败与修复体验 | P1 | Required | T08、T09 | ☐ |
| T11 | 增加报告完成后的后续动作 | P1 | Required | T09 | ☐ |
| T12 | 完善周期性报告更新保护 | P1 | Required | T11 | ☐ |
| T13 | 建立 Web、CLI 和发布回归测试 | P0 | Required | T05、T07、T09、T12 | ☐ |
| T14 | 发布前验收与回滚准备 | P0 | Required | T13 | ☐ |
| T15 | 实现匿名漏斗观测 | P2 | Optional | T14 | ☐ |
| T16 | 增加样例报告复用模板 | P2 | Optional | T12 | ☐ |
| T17 | 建立用户案例和模板展示 | P2 | Optional | T05、T14 | ☐ |
| T18 | 增加轻量桌面/非 Agent 入口调研 | P2 | Optional | T14 | ☐ |

## 4. 必需任务

### T01 [Required] 建立增长基线与验证口径

**描述**：确认当前官网、GitHub Release、npm、Plugin 和首次 Report 路径的现状，建立上线前基线。

**依赖**：无。

**预期产出**：

- 一份当前漏斗记录：访问、示例打开、安装点击、安装验证、首次生成成功/失败。
- 按来源、宿主环境和安装方式区分的统计口径。
- 一份当前安装和首次成功的手工测试记录。
- 明确哪些指标是真实事件，哪些是估算值。

**完成条件**：产品负责人可以用同一口径比较上线前后 30 天数据；不要求先建设远程埋点。

### T02 [Required] 建立版本与安装内容单一来源

**描述**：减少官网、Skill、CLI、Release 文档之间的版本号和命令漂移。

**建议范围**：

- 检查 `packages/miao-viz-cli/package.json`、`package.json`、`skills/miao-vision/cli-compatibility.json` 和发布工作流。
- 生成或校验供 Web 使用的安装 manifest。
- 校验 Plugin、Skill ZIP、npm CLI 和 Release tag 的版本一致性。

**依赖**：T01。

**预期产出**：

- 可被官网安装区读取的版本/宿主配置。
- 发布时失败即中止的版本一致性检查。
- 当前 CLI 分组命令的权威列表。

**完成条件**：修改兼容版本后，构建检查可以发现官网或安装文档中的过期值；官网不再手工散落维护同一版本号。

### T03 [Required] 创建 Report 首次体验包

**描述**：提供一套可以从官网直接下载、复制和复现的销售/经营 Report 示例。

**建议范围**：

- 样例 CSV，包含时间字段、分类字段和可聚合度量。
- 与样例数据匹配的自然语言提示词。
- 可打开的自包含 HTML Report。
- 样例使用说明和预期输出说明。

**依赖**：T02。

**预期产出**：一个版本化的 `ExamplePackage`，包括 `sampleDataUrl`、`prompt`、`artifactUrl`、CLI 版本和证据能力标记。

**完成条件**：从干净环境按照提示词运行，能够生成与示例场景一致的 HTML；数据、提示词和产物不互相矛盾。

### T04 [Required] 重构首屏为 Report 主入口

**描述**：把官网首屏从“介绍多种技术能力”调整为“Excel/CSV 生成可分享业务报告”。

**目标页面**：`apps/web/src/components/LandingPage.svelte` 及相关样式。

**预期产出**：

- 结果导向标题和副标题。
- “生成第一份报告”主 CTA。
- “查看真实报告”次 CTA。
- 首屏可见的本地、无账号、无需上传说明。
- Deck 和 Infographic 降为次级展示，不与 Report 争夺主 CTA。

**依赖**：T01。

**完成条件**：首次访问者无需阅读 CLI 或 VizSpec 术语即可理解输入、输出和隐私边界；主 CTA 直接进入首次 Report 或安装路径。

### T05 [Required] 改造示例卡片与示例动作

**描述**：把示例从静态展示卡片改造成激活入口。

**目标页面**：`LandingPage.svelte`、示例卡片组件、相关 CSS 和公共示例资产。

**预期产出**：

- Report 示例提供“打开报告”“下载样例数据”“复制提示词”。
- 复制成功反馈和剪贴板不可用时的可选中文本。
- 示例页显示离线可用、产物类型和证据说明能力。
- 从示例页进入安装分流。

**依赖**：T03、T04。

**完成条件**：用户可以不安装任何东西打开示例、下载数据、复制提示词；桌面和约 390px 移动端动作均可访问。

### T06 [Required] 设计宿主安装分流模型

**描述**：定义 Codex、Claude Code、OpenClaw 和纯 CLI 的安装数据结构及推荐路径。

**预期产出**：

- `HostId` 和 `InstallOption` 类型或等价 schema。
- 每个宿主一个推荐路径，备用路径明确标识。
- Plugin 与 CLI 的关系、权限和联网行为说明。
- 每个路径的验证命令和备用安装方式。

**依赖**：T02。

**完成条件**：产品和开发可以根据模型直接生成安装区，不需要在组件内硬编码宿主判断或重复版本信息。

### T07 [Required] 实现安装分流与验证文案

**描述**：在安装区实现宿主选择、推荐安装步骤、版本说明和验证步骤。

**目标页面**：`apps/web/src/components/InstallSection.svelte`、相关样式和安装文档。

**预期产出**：

- `InstallRouter` 或等价的 Svelte 组件。
- Codex、Claude Code、OpenClaw、CLI 四组内容。
- 稳定 Release 下载按钮和当前版本显示。
- `miao-viz` 绝对路径/版本验证命令。
- 不再默认要求用户同时安装 Plugin 和全局 CLI。

**依赖**：T06。

**完成条件**：每个宿主显示一个明确推荐路径；纯 CLI 用户可以独立完成安装；安装文案与当前 CLI `--help` 的分组命令一致。

### T08 [Required] 实现统一环境诊断

**描述**：为“缺 CLI、版本不兼容、Node 不兼容、宿主不可用、输入不可读、输出不可写、PDF 依赖缺失”等问题提供统一诊断结果。

**建议范围**：扩展 `skills/miao-vision/scripts/check-miao-viz.mjs`，或新增 CLI `diagnose` 子命令；最终只能保留一个用户推荐入口。

**预期产出**：

- `DiagnosticResult` 接口和稳定诊断码。
- CLI 绝对路径、CLI 版本、Node 版本和宿主信息。
- 文件可访问性和输出目录可写性检查。
- 每个错误的安全修复建议和 `safeToRetry` 标识。
- 敏感数据过滤规则。

**依赖**：T02、T07。

**完成条件**：诊断能区分至少六类核心错误；不打印原始数据、令牌、密钥或文件内容；拒绝自动下载权限时仍提供手动修复路径。

### T09 [Required] 打通五分钟首次 Report 冒烟路径

**描述**：用 T03 样例验证从宿主请求到 HTML 交付的完整路径。

**执行链路**：

```text
resolve CLI
→ data analyze
→ spec/block instantiate
→ spec validate --context --verify --strict
→ render report --format html
→ delivery manifest
```

**依赖**：T03、T07。

**预期产出**：

- 一条自动化 workflow smoke test。
- 成功时返回绝对产物路径和 `DeliveryManifest`。
- 失败时保留阶段、错误码和可重试信息。
- 记录从开始到 HTML 交付的耗时。

**完成条件**：样例在正常环境下中位耗时不超过 5 分钟；用户不需要手写 YAML；生成结果是自包含 HTML。

### T10 [Required] 完善结构化失败与修复体验

**描述**：确保失败不会变成无法行动的自由文本错误。

**覆盖场景**：字段不存在、格式损坏、证据路径错误、验证失败、阻止图表、输出目录不可写、PDF 依赖缺失。

**预期产出**：

- 稳定错误码、`message`、`details`、`retryable` 和 `patches`。
- Agent 可以根据 patch hints 重试 Spec 验证。
- HTML 成功但 PDF 失败时，HTML 仍然交付。
- 旧报告在更新失败时保持不变。

**依赖**：T08、T09。

**完成条件**：错误信息包含发生阶段、受影响对象、下一步、是否可重试和是否已有可用产物；失败用例测试通过。

### T11 [Required] 增加报告完成后的后续动作

**描述**：在交付 manifest 和 Agent 回复中提供打开、分享、更新三个明确后续动作。

**预期产出**：

- `DeliveryManifest.summary.actions` 包含三个动作或等价信息。
- HTML 报告展示证据说明入口。
- 成功回复返回正式产物绝对路径。
- “使用新数据更新”提示引用当前项目/spec，而不要求用户重新设计报告。

**依赖**：T09。

**完成条件**：首次成功后，用户无需重新阅读完整文档即可知道如何打开报告、分享文件和开始下一周期更新。

### T12 [Required] 完善周期性报告更新保护

**描述**：保证新数据更新会复用结构，并在字段变化或不可比较时阻止误导性结论。

**目标范围**：`report-project-storage.ts`、`report-changes.ts`、`period-outcome-*` 及相关测试。

**预期产出**：

- `ReportProject` 保存 Spec、指标配方、证据配方、布局、主题和数据指纹。
- `PeriodChanges` 返回新增、删除、变化字段以及 `notComparable` 对象。
- 新产物写入独立周期目录。
- 更新失败不覆盖旧产物。

**依赖**：T11。

**完成条件**：成功更新保留旧报告；缺字段时返回受影响对象；不可比较指标不会生成同比、环比或趋势结论。

### T13 [Required] 建立 Web、CLI 和发布回归测试

**描述**：将 requirements 和 design 中的验收条件固化为自动化测试。

**测试范围**：

- Web：首屏 CTA、示例动作、宿主分流、复制降级、移动端可用性。
- CLI：四种输入格式、完整 Report workflow、证据验证、错误码和 patch hints。
- 更新：成功、缺字段、不可比较、旧产物保留。
- 发布：版本、Release 资产、Skill/Plugin/CLI 一致性。

**依赖**：T05、T07、T09、T12。

**预期产出**：Vitest、Playwright 和发布校验测试，以及测试夹具和临时目录清理策略。

**完成条件**：通过以下命令，且不编辑生成目录作为测试源：

```bash
npm run check:size
npm run check
npm run test:run
npm run test:skill-runtime
npm run build:cli
npm run validate:plugin
npm run build
```

### T14 [Required] 发布前验收与回滚准备

**描述**：在发布前执行完整手工验收，并准备静态官网和 CLI 兼容性的回滚方案。

**依赖**：T13。

**预期产出**：

- 桌面和 390px 移动端验收记录。
- 四种宿主安装路径验收记录。
- 样例首次 Report 五分钟验收记录。
- 离线 HTML、证据面板和周期更新验收记录。
- 上一版官网构建产物、CLI 版本和文档回滚方式。

**完成条件**：所有 Required 验收标准有通过/失败记录；发现失败时能独立回滚官网，而不删除用户 CLI 或已有报告。

## 5. 可选任务

### T15 [Optional] 实现匿名漏斗观测

**描述**：增加不包含原始数据的增长事件记录和来源归因。

**依赖**：T14。

**预期产出**：

- `FunnelEvent` schema。
- `landing_view`、`example_open`、`install_click`、`install_verify`、`first_report_start`、`first_report_success`、`first_report_failure`、`repeat_report_start` 事件。
- 事件按来源、宿主、安装方式、CLI 版本和错误类别分组。
- CLI 默认本地 JSONL/标准输出方案；远程汇总为明确可选项。

**完成条件**：事件采集失败不影响官网、安装或报告生成；不记录文件内容、文件名、敏感路径、密钥或令牌。

### T16 [Optional] 增加样例报告复用模板

**描述**：把首个销售 Report 扩展为周报、月报、经营报告、投放报告和项目进度报告模板。

**依赖**：T12。

**预期产出**：每个模板包含样例数据要求、提示词、输出示例、适用场景和不可用条件。

**完成条件**：用户可以从同一模板结构生成首份报告并执行下一周期更新；模板不引入未验证指标。

### T17 [Optional] 建立用户案例和模板展示

**描述**：在官网增加可复制的真实使用案例和用户产物展示。

**依赖**：T05、T14。

**预期产出**：案例页面或案例区，每个案例包含输入类型、任务、产物、提示词和本地隐私说明。

**完成条件**：案例均可脱离用户敏感数据展示，且每个案例可直接跳转到样例或安装路径；不把未经授权的用户数据上传或公开。

### T18 [Optional] 增加轻量桌面/非 Agent 入口调研

**描述**：验证“目标用户愿意通过可执行本地命令的 Agent 宿主”这一关键假设。

**依赖**：T14。

**预期产出**：一份基于用户访谈、首次成功率和安装失败原因的决策报告，比较纯 CLI、轻量桌面壳和 Agent Plugin 的首次成功成本。

**完成条件**：给出继续 Agent-first、优化纯 CLI 或建设桌面入口中的一个明确建议；不在调研任务中直接引入新运行时或后端。

## 6. 依赖规则与并行执行

### 6.1 可并行任务

在 T01 完成后，以下任务可以并行：

- T02：版本和安装内容。
- T04：首屏信息架构。

T02 完成后，以下任务可以并行：

- T03：示例体验包。
- T06：安装分流模型。

T03 和 T04 完成后执行 T05；T06 完成后执行 T07。T08 和 T09 必须在安装模型稳定后开始，但两者可在实现层面并行推进。

### 6.2 不得跳过的依赖

- 不得在没有 T03 的情况下声称完成“首次体验闭环”。
- 不得在没有 T02 的情况下继续复制版本号到新文案或组件。
- 不得在没有 T09 的情况下声称满足五分钟首次成功目标。
- 不得在没有 T12 的情况下宣传周期性报告更新。
- 不得在没有 T13 和 T14 的情况下发布增长相关官网改动。

## 7. Definition of Done

### Required 版本

满足以下条件才可标记为首个增长优化版本完成：

- T01–T14 全部完成。
- 官网以 Report 为主入口，且示例可打开、下载和复制提示词。
- 四种宿主安装路径清晰，纯 CLI 路径独立可用。
- 样例 Report 正常路径五分钟内完成。
- Spec、证据、文件系统和 PDF 失败都有结构化处理。
- 周期更新不覆盖旧报告，并正确标记不可比较结果。
- 自动化检查和手工验收全部通过。

### Optional 扩展

T15–T18 不阻断首个版本，但至少完成 T14 后再选择是否执行。若 T14 显示首次成功率低于 60%，应优先修复 T08–T10，而不是先做案例展示、更多模板或桌面入口。

## 8. 回滚边界

- 官网内容或组件回归：回滚 Web 静态构建产物，不影响本地 CLI、Plugin 或已有 HTML。
- CLI 兼容性回归：恢复上一版 CLI/Plugin 兼容配置，不删除用户目录中的 CLI。
- 周期更新回归：保留旧项目和旧产物，停止新周期写入。
- 匿名事件问题：关闭事件采集或恢复本地记录，不阻断核心工作流。
