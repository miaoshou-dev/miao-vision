# Miao Vision 增长与首次使用优化需求

## 1. 文档目的

本文将产品讨论结论转化为可验收的需求，目标是提高 Miao Vision 的：

1. 官网访问者到安装动作的转化率。
2. 安装用户到首次成功生成报告的转化率。
3. 首次成功用户在 7 日内再次使用的比例。
4. 一次性报告用户转化为周期性报告用户的比例。

## 2. 产品方向与范围

### 2.1 核心定位

Miao Vision 是一个本地优先的 AI 数据报告生成工具：用户提供 CSV、TSV、XLSX 或 JSON 文件，并用自然语言描述目标，最终获得可分享的 HTML 或 PDF 业务报告。

首要传播场景为：

> 用一句话，把 Excel/CSV 变成一份可直接分享的业务报告。

Data Report 是默认主路径。Browser Deck 和 Article Infographic 作为次级能力展示，不与 Report 在首屏争夺主行动。

### 2.2 范围内

- 官网首屏价值表达和主要行动按钮。
- 示例报告、样例数据、提示词和安装入口之间的闭环。
- Codex、Claude Code、OpenClaw 和纯 CLI 用户的安装分流。
- 安装后的版本检查、环境诊断和首次成功引导。
- 报告完成后的“使用新数据更新”引导。
- 下载、安装、首次成功和再次使用的匿名漏斗指标。

### 2.3 范围外

- 将官网改造成在线数据分析或 SQL Workspace。
- 将用户数据上传到服务器或建立必需的云端后端。
- 在 CLI 中新增 URL 抓取、在线数据库连接或实时 Dashboard。
- 在本需求中增加新的图表类型或替换现有渲染引擎。
- 在首次验证增长前建设团队协作、账号体系或复杂计费系统。

## 3. 成功标准

上线后以最近 30 天数据与上线前 30 天数据比较，满足以下目标：

| 指标 | 目标 |
|---|---:|
| 官网到安装动作转化率 | 提升 30% |
| 安装到首次成功生成报告 | ≥ 60% |
| 首次成功耗时中位数 | ≤ 5 分钟 |
| 首次成功后 7 日内再次使用 | ≥ 25% |
| 报告生成失败后得到可执行修复建议的比例 | ≥ 90% |

指标必须区分来源（官网、GitHub、npm、社区文章）、宿主环境和安装方式。若无法采集真实安装完成事件，必须使用本地匿名事件或用户主动复制的诊断结果作为替代，并明确标记为估算值。

## 4. 用户画像与关键任务

### 4.1 主要用户

- 需要快速产出经营、销售、投放或项目报告的业务人员。
- 使用 Codex 或 Claude Code，希望让 Agent 生成可交付数据产物的开发者和分析师。

### 4.2 关键任务

用户需要在不学习 VizSpec、不手写图表代码、不上传敏感数据的情况下：

1. 判断 Miao Vision 是否适合自己的文件和工作环境。
2. 安装正确的 Plugin 或 CLI。
3. 用样例或自己的文件生成第一份可信报告。
4. 验证报告中的数字有来源且可以离线分享。
5. 在下一周期用新文件复用相同的指标和版式。

## 5. User stories 与验收标准

### US-01：理解产品价值

**作为**第一次访问官网的潜在用户，**我希望**在首屏看到输入、输出和隐私边界，**以便**在 10 秒内判断是否值得安装。

验收标准：

- 首屏明确出现“CSV/Excel → 可分享业务报告”的结果导向表达。
- 首屏同时说明本地优先、无账号、无需上传数据。
- 首屏存在一个主行动“生成第一份报告”或等价文案，以及一个次行动“查看真实报告”。
- Report 是首屏唯一主推荐路径，Deck 和 Infographic 不得与其使用同等级主按钮。
- 用户无需阅读 CLI、VizSpec 或 Agent 实现细节即可理解产品用途。

### US-02：浏览并复用示例

**作为**尚未安装的访客，**我希望**先看到真实报告并获得样例数据和提示词，**以便**低风险地体验完整结果。

验收标准：

- 示例报告卡片至少提供“打开示例”“下载样例数据”“复制提示词”三个动作中的前两个；Report 示例必须提供全部三个动作。
- 样例数据、提示词和示例产物使用同一业务场景，默认使用销售或经营报告场景。
- 示例页明确显示生成后的产物类型、是否可离线打开、是否包含证据说明。
- 复制提示词后显示成功反馈；复制失败时提供可选中的纯文本内容。
- 用户可以从示例页直接进入对应宿主环境的安装入口。

### US-03：选择正确安装方式

**作为**潜在用户，**我希望**根据自己的 Agent 环境看到唯一清晰的安装路径，**以便**避免同时安装不必要的组件。

验收标准：

- 安装区按 Codex、Claude Code、OpenClaw、纯 CLI 分组。
- 每个分组只标记一个推荐路径；其他兼容路径必须折叠或标为备用。
- 页面明确解释 Plugin 是 Agent 接口，CLI 是本地执行引擎，以及两者是否由 Plugin 自动获取。
- 下载按钮使用稳定的 Release 资源链接；命令示例使用当前 CLI 分组命令，不得出现已经废弃的顶层命令。
- 安装区显示当前兼容版本、支持平台、权限说明和数据本地化说明。
- 纯 CLI 用户可以在不安装 Agent Plugin 的情况下完成 CLI 安装和验证。

### US-04：五分钟内首次成功

**作为**已安装用户，**我希望**只复制一句自然语言提示并附加一个文件就能生成报告，**以便**快速判断工具是否有效。

验收标准：

- 安装完成后，用户获得至少一条可直接复制的 Report 提示词。
- 提示词包含输入文件、报告目标、输出格式和“数字必须基于数据证据”的要求。
- Agent 能按 Analyze → Spec → Validate → Render 顺序完成流程，用户不需要手写 YAML。
- 成功时返回 HTML 产物的绝对路径，并说明如何打开或分享。
- 从用户开始执行到获得 HTML 产物的正常路径中位时长不超过 5 分钟。
- 失败时返回人类可读原因、机器可读错误码和下一步修复动作。

### US-05：信任报告结果

**作为**需要把报告发给同事或管理层的用户，**我希望**看到数字的来源和隐私边界，**以便**放心使用和分享。

验收标准：

- 报告中的 KPI、图表和洞察保留可验证的证据关联。
- 报告提供业务可读的证据说明，至少包含计算口径、范围、筛选条件、样本量和验证状态。
- HTML 产物默认不依赖服务器、登录或 CDN 即可打开。
- 官网和安装文档不得暗示数据会被上传到 Miao Vision 服务。
- 当报告无法完成严格证据验证时，不得把产物标记为已验证；必须展示警告或阻止发布。

### US-06：复用周期性报告

**作为**每周或每月重复产出报告的用户，**我希望**用新文件更新原有报告结构，**以便**保持指标、布局和主题一致。

验收标准：

- 首次报告完成后展示“下次用新数据更新”的下一步建议。
- 用户可以保存或引用上次报告的指标、证据配方、布局和主题。
- 新旧数据字段不兼容时，系统指出具体字段和不可比较的指标，不得静默替换。
- 更新成功时说明哪些指标可比较、哪些发生变化、哪些被跳过。
- 更新失败不会破坏上一份已生成的报告或项目文件。

### US-07：诊断安装问题

**作为**安装失败或环境不兼容的用户，**我希望**得到针对当前环境的诊断，**以便**不必在多个文档之间猜测原因。

验收标准：

- 用户可以执行一个诊断命令查看当前使用的 `miao-viz` 绝对路径和版本。
- 诊断结果至少检查 Node.js、CLI 版本、宿主插件可用性和输入文件可访问性。
- 诊断结果区分“缺少 CLI”“版本不兼容”“权限拒绝”“文件不存在”“字段不存在”和“导出依赖缺失”。
- 每类错误提供一个安全、可复制的修复建议。
- 诊断不得打印数据内容、令牌、密钥或完整的敏感文件路径之外的文件内容。

## 6. System behaviors（EARS）

### 6.1 官网与分发

- **WHEN** 用户打开官网首屏，**THE SYSTEM SHALL** 在首屏展示“本地文件生成可分享业务报告”的核心价值、输入格式、输出格式和本地隐私承诺。
- **WHEN** 用户点击主行动按钮，**THE SYSTEM SHALL** 将用户带到 Report 首次体验或对应安装路径，而不是仅滚动到泛化的功能介绍。
- **WHEN** 用户选择 Codex、Claude Code、OpenClaw 或 CLI，**THE SYSTEM SHALL** 只展示该环境的推荐安装路径，并保留备用路径的明确标识。
- **WHEN** 当前发布版本发生变化，**THE SYSTEM SHALL** 从单一版本来源更新官网安装文案、安装文档和兼容性提示。
- **WHEN** 用户点击下载插件，**THE SYSTEM SHALL** 指向稳定的最新 Release 资源，并在下载区域显示版本和校验说明。
- **WHEN** 用户复制命令或提示词，**THE SYSTEM SHALL** 提供成功状态反馈；**IF** 剪贴板不可用，**THE SYSTEM SHALL** 保证文本可选中复制。

### 6.2 首次体验

- **WHEN** 用户选择“首次生成报告”，**THE SYSTEM SHALL** 提供一组已验证的样例数据、提示词和预期产物。
- **WHEN** 用户附加 CSV、TSV、XLSX 或 JSON 文件并提出 Report 请求，**THE SYSTEM SHALL** 先检查文件可访问性和格式，再进入 Analyze → Spec → Validate → Render 流程。
- **WHEN** Agent 需要选择字段或图表，**THE SYSTEM SHALL** 让 Agent 使用分析上下文中的字段、证据和允许图表目录。
- **WHEN** 报告 Spec 未通过验证，**THE SYSTEM SHALL** 返回结构化错误和可执行 patch hints，并且不得发布为已验证产物。
- **WHEN** 报告 Spec 通过严格验证，**THE SYSTEM SHALL** 生成自包含 HTML，并返回正式产物路径。
- **WHEN** HTML 生成成功，**THE SYSTEM SHALL** 展示打开、分享和使用新数据更新的下一步动作。

### 6.3 可信度与隐私

- **WHEN** 报告包含 KPI、图表或洞察，**THE SYSTEM SHALL** 为每个对象保留证据或明确的非数据声明来源。
- **WHEN** 证据路径不存在、数值不匹配或声明检查失败，**THE SYSTEM SHALL** 返回对应结构化错误码，并阻止严格发布。
- **WHEN** 用户生成 HTML 报告，**THE SYSTEM SHALL** 默认不要求登录、远程数据库、后端服务或 CDN。
- **WHEN** 系统输出诊断或错误信息，**THE SYSTEM SHALL NOT** 输出用户文件的原始数据、API 密钥、访问令牌或无关目录内容。

### 6.4 周期性更新

- **WHEN** 用户请求使用新文件更新已有报告，**THE SYSTEM SHALL** 复用已保存的指标、证据配方、布局和主题。
- **WHEN** 新文件缺少已有报告依赖的字段，**THE SYSTEM SHALL** 返回缺失字段列表和受影响的指标或图表。
- **WHEN** 两个周期的数据不可比较，**THE SYSTEM SHALL** 将受影响结果标记为 `notComparable`，并且不得生成误导性的同比或环比结论。
- **WHEN** 周期性更新成功，**THE SYSTEM SHALL** 生成新的独立产物，并保留上一份产物不变。

## 7. Functional requirements

### FR-01 首屏与信息架构

- 官网首屏必须以 Report 为默认主任务。
- 首屏必须包含结果导向标题、输入/输出说明、隐私承诺、主 CTA 和示例 CTA。
- 功能介绍顺序必须为：真实产物 → 首次体验 → 安装 → 可信度 → 其他产物类型。
- 27 种图表、VizSpec 和验证机制作为证明材料展示，不得作为用户首次理解产品的前置知识。

### FR-02 示例体验包

- 仓库必须维护一套与示例报告匹配的样例数据。
- 样例数据必须覆盖至少一个时间字段、一个分类字段和一个可聚合度量，保证默认 Report 流程可运行。
- 示例必须包含一条自然语言提示词和一个可打开的 HTML 结果。
- 示例数据、提示词、报告和文档中的命令必须由同一版本兼容性检查覆盖。

### FR-03 安装入口

- 安装页必须提供环境选择器或等价分组导航。
- Plugin 安装和 CLI 安装必须在文案上明确依赖关系。
- 安装页必须提供验证步骤，验证内容包括版本和可执行路径。
- 任何版本号不得在多个文件中无约束地手工复制；发布流程必须能检查版本一致性。
- 不得要求用户同时执行 Plugin 安装和全局 CLI 安装，除非诊断结果明确说明需要备用路径。

### FR-04 首次生成报告

- 首次 Report 路径必须支持 CSV、TSV、XLSX 和 JSON。
- Agent 不得要求用户手写 Spec 才能完成默认样例流程。
- CLI 必须继续返回 `{ ok: true, value }` 或 `{ ok: false, code, message, ... }` 结构。
- 验证失败必须保留 `code`、证据错误信息和 patch hints。
- 成功结果必须返回 HTML 产物路径；用户请求 PDF 时，必须明确说明可选浏览器依赖及失败原因。

### FR-05 结果分享与复用

- HTML 报告必须是自包含、离线可打开的正式产物。
- 报告必须提供面向业务用户的证据说明入口。
- 完成页或 Agent 回复必须提供“打开报告”“分享文件”“用新数据更新”三个后续动作。
- 周期性更新必须产生新产物，不得覆盖上一周期产物。

### FR-06 诊断与指标

- 必须记录或可推导以下漏斗事件：`landing_view`、`example_open`、`install_click`、`install_verify`、`first_report_start`、`first_report_success`、`first_report_failure`、`repeat_report_start`。
- 事件不得包含原始数据、文件内容、密钥或完整的敏感路径。
- 事件至少携带宿主环境、安装渠道、CLI 版本和错误类别；无法确认的字段使用明确的 `unknown` 值。
- 用户可以通过诊断命令导出本地结果，不得以联网为成功前提。

## 8. Edge cases and error handling

| 场景 | 系统行为 | 用户可见结果 |
|---|---|---|
| 用户只想看示例，不想安装 | 允许打开示例和下载样例数据 | 不强制弹出安装或注册要求 |
| 用户没有 Codex/Claude 环境 | 展示纯 CLI 路径 | 明确说明可直接安装 CLI |
| 用户只安装了 Plugin，CLI 尚未就绪 | 运行兼容性检查并提示获取 CLI | 显示版本、权限和下一步修复动作 |
| CLI 已存在但版本不兼容 | 阻止使用不兼容版本 | 返回当前版本、要求版本和升级命令 |
| GitHub Release 暂时不可达 | 不伪造下载成功状态 | 提供 npm 或已下载 ZIP 的备用路径 |
| 用户拒绝 CLI 下载权限 | 停止自动安装 | 提供手动安装和验证命令 |
| 剪贴板 API 不可用 | 保留可选中文本 | 显示“请手动复制”提示 |
| 输入文件不存在或无权限 | 在 Analyze 前终止 | 返回 `FILE_NOT_FOUND` 或权限错误及路径检查建议 |
| 文件格式不支持或文件损坏 | 不进入渲染 | 返回支持格式和重新导出建议 |
| 空文件或没有可分析字段 | 不生成空洞报告 | 返回需要的字段类型和样例要求 |
| 字段名含空格、中文或特殊字符 | 使用真实 profile 字段名 | 不允许 Agent 静默猜测字段名 |
| 数据没有时间字段但用户要求趋势 | 降级为可用的分类分析或请求澄清 | 明确说明无法生成时间趋势，不伪造时间维度 |
| 样本量过小或数据质量异常 | 保留警告并降低结论强度 | 报告显示样本量和数据质量提示 |
| Spec 引用不存在的证据路径 | 阻止严格发布 | 返回证据路径、对象位置和 patch hint |
| 图表类型被目录阻止 | 不渲染该图表 | 返回允许图表及阻止原因 |
| PDF 浏览器依赖未安装 | HTML 仍可生成 | 单独报告 PDF 不可用和安装依赖方式 |
| 周期更新缺少字段 | 不覆盖旧报告 | 返回缺失字段和受影响对象 |
| 周期更新指标不可比较 | 标记 `notComparable` | 不生成同比、环比或趋势结论 |
| 输出目录不可写 | 切换到任务级临时目录或停止 | 明确告知最终产物路径，不静默丢失文件 |
| 生成过程中断 | 保留旧产物，清理不完整的新产物 | 返回可重试的阶段和错误码 |
| 用户请求敏感数据诊断 | 只返回元数据和错误类别 | 不输出原始行、令牌或文件内容 |

## 9. 非功能要求

- 本地数据处理必须默认不上传数据，不依赖后端才能完成核心流程。
- 官网必须能在桌面和约 390px 宽移动端清晰完成“查看示例”和“开始安装”。
- 安装文案、CLI 帮助、Skill 文档和 Release 资产必须保持版本与命令一致。
- CLI 核心源码和 Svelte 源文件继续遵守仓库的 500 行文件限制。
- 变更不得编辑 `apps/web/dist/` 或 `packages/miao-viz-cli/dist/` 等生成目录作为源文件。

## 10. 验证要求

### 自动化验证

```bash
npm run check:size
npm run check
npm run test:run
npm run build:cli
npm run build
```

### 手工验收

1. 从官网首屏进入示例，打开报告、下载样例数据、复制提示词。
2. 分别检查 Codex、Claude Code、OpenClaw 和纯 CLI 的推荐安装路径。
3. 使用样例数据在 5 分钟内生成 HTML 报告。
4. 使用错误字段、损坏文件、缺失 CLI 和不兼容版本验证错误提示。
5. 验证报告中的证据面板、离线打开和产物路径。
6. 使用第二份数据执行周期性更新，确认旧报告不被覆盖且不可比较指标被标记。
7. 在桌面和约 390px 移动端检查 CTA、示例动作和安装分组的可用性。

## 11. 发布与回滚

- 每个阶段必须可以独立发布；首阶段只需完成首屏、示例体验包和安装分流即可产生价值。
- 官网文案或安装入口出现回归时，可回滚到上一版静态官网，不影响已经安装的 CLI 和已有 HTML 产物。
- CLI 版本兼容性检查失败时，必须保留用户现有 CLI 和报告文件，不自动删除或覆盖。
- 新的匿名指标只能记录元数据；若指标方案引入隐私或合规风险，应关闭采集而不阻断本地报告生成。
